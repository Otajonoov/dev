- Unbuffered vs buffered channel
- Directional channels (`chan<-`, `<-chan`)
- `close()` va range over channel
- `select` statement — random selection, `default` case
- Channel patterns: fan-out, fan-in, pipeline, done channel, semaphore