- **Array** — fixed size, value type
- **Slice** — header tuzilishi (`ptr + len + cap`), `make`, `append`, `copy`, slice tricks
- **Map** — `make`, iteration order, nil map, concurrent read/write
- **Struct** — embedding, anonymous fields, tags
- **Pointer** — `*`, `&`, `new()`, pointer receiver vs value receiver