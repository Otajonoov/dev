- Multiple return values
- Named return values
- Variadic functions (`...`)
- Function as first-class citizen
- **Closure** — variable capture, loop variable trap
- **defer** — LIFO order, defer + named return, defer overhead
- **panic / recover** — stack unwinding, recover faqat defer ichida ishlaydi

