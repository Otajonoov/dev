- Tri-color mark & sweep algoritmi
- Write barrier — nima uchun kerak
- GC phases: mark setup → concurrent mark → mark termination → sweep
- `GOGC` environment variable
- `GOMEMLIMIT` (Go 1.19+)
- `runtime.GC()`, `runtime.ReadMemStats()`
- GC pausing va latency optimization
- Finalizer — `runtime.SetFinalizer`