- **Allocator** tuzilishi: `mheap → mcentral → mcache → span`
- Size classes (8B dan 32KB gacha)
- Large object allocation (>32KB) — to'g'ridan-to'g'ri `mheap`
- Stack allocation vs heap allocation
- `new()` vs `make()` farqi internals jihatdan