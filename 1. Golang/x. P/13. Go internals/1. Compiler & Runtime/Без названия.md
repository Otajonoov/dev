- Go compilation pipeline: parsing → type checking → SSA → machine code
- `go build`, `go tool compile`, `go tool objdump`
- Inlining — qachon inline qiladi, `//go:noinline`
- Escape analysis — stack vs heap allokatsiya
- `go build -gcflags="-m"` — escape analysis output