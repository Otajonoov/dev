- Worker pool
- `errgroup` (`golang.org/x/sync/errgroup`)
- `singleflight` (`golang.org/x/sync/singleflight`)
- Rate limiting (`time.Ticker` + channel)
- Timeout va cancellation patterns
- `context` + goroutine kombinatsiyasi