**Muammo**: Ilova ishlamayapti, lekin sistema buni sezmayapti.

**Yechim**: Health endpoint (`/healthz`) orqali holatini tekshirib turish.

**💡 Kubernetes’da:**
```
livenessProbe:
  httpGet:
    path: /healthz
    port: 8080
```
