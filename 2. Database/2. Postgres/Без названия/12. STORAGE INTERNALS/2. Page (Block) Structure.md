Har bir relation file — 8KB pagelardan iborat: ``` +------------------+ | PageHeader | -- LSN, checksum, flags, pd_lower, pd_upper +------------------+ | ItemId array | -- (offset, flags, length) per tuple +------------------+ | Free Space | +------------------+ | Tuples (Items) | -- actual data, yuqoridan pastga +------------------+ | Special Space | -- index uchun (B-tree sibling pointers) +------------------+ ``` **PageHeader muhim fieldlar:** - `pd_lsn` — last WAL record LSN - `pd_lower` — ItemId arrayning oxiri - `pd_upper` — tuple areaning boshi - `pd_special` — special area boshi - `pd_checksum` — data integrity

