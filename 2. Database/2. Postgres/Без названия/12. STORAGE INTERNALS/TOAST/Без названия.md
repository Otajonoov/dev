TOAST (The Oversized-Attribute Storage Technique)
  
- Tuple 8KB page ga sig'masa → TOAST ishlatiladi
- Threshold: 2KB (default `toast_tuple_target`)
- **Compression** — pglz yoki lz4 (PostgreSQL 14+)
- **Out-of-line storage** — alohida TOAST table (`pg_toast_NNNN`)
- **External** — compress qilinmagan out-of-line
- Storage strategies: `PLAIN`, `EXTENDED`, `EXTERNAL`, `MAIN`