- B-tree page tuzilishi: `ItemId[]` + `Item[]` + header
- Page size: 8KB (default)
- Fill factor — `fillfactor=70` hot updates uchun
- Index bloat — `VACUUM` va `REINDEX`
- Visibility map — index-only scan uchun