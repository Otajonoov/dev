- **Sequential Scan** — to'liq table scan
- **Index Scan** — index + heap fetch
- **Index Only Scan** — faqat index (visibility map yordamida)
- **Bitmap Index Scan** — bitmap + Bitmap Heap Scan (OR conditions)
- **TID Scan** — tuple ID bo'yicha to'g'ridan-to'g'ri