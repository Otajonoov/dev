- `JSON` vs `JSONB` — farqlari (JSONB: binary, indexed, deduplication)
- `->`, `->>`, `#>`, `#>>`
- `jsonb_set`, `jsonb_insert`, `||` (merge)
- `jsonb_agg`, `jsonb_object_agg`
- `jsonb_path_query` — JSONPath