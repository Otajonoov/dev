- `SMALLINT`, `INTEGER`, `BIGINT`
- `NUMERIC(p, s)` / `DECIMAL` — exact arithmetic
- `REAL`, `DOUBLE PRECISION` — floating point
- `SERIAL`, `BIGSERIAL` — deprecated, o'rniga `IDENTITY`
- `GENERATED ALWAYS AS IDENTITY`