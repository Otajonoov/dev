- `CREATE TABLE` — constraints: `PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, `CHECK`, `NOT NULL`, `DEFAULT`
- `ALTER TABLE` — column qo'shish/o'chirish, constraint o'zgartirish
- `DROP`, `TRUNCATE` — farqlari
- `TEMP TABLE`, `UNLOGGED TABLE`